<?xml version="1.0" encoding="utf-8" ?>
<Patch>
	<!-- Example Optional Patch -->
	<!-- patchName - this is the link between the patch and its stored setting -->
	<!-- label - the name of the setting in mod options -->
	<!-- toolTip - additional info displayed when hovered over -->
	<!-- log - log creation of new settings -->
	<!-- enabled - patchoperation to run when enabled -->
	<!-- disabled - patchoperation to run when disabled -->
	<!-- enabledByDefault - Patch is enabled by default, defaults to true -->
	<!-- 
	<Operation Class="PatchOperationOptional">
		<patchName>name</patchName>
		<label>label</label>
		<toolTip>toolTip</toolTip>
		<log>true</log>
		<enabledByDefault>true</enabledByDefault>
		<mods>
			<li></li>
		</mods>
		<enabled Class="PatchOperationSequence">		
			<operations>	
			
			</operations>	
		</enabled>	
		<disabled Class="PatchOperationSequence">		
			<operations>	
			
			</operations>	
		</disabled>	
	</Operation>
	-->
</Patch>